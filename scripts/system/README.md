# System Scripts

> Scripts related to system configuration, maintenance, and security hardening.   

## Usage

Each script should be run with appropriate permissions (usually as `root` or using `sudo`):   

```bash
sudo ./<script-name>.sh
```
