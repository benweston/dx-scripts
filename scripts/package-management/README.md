# Package Management Scripts

> Scripts related to installing repositories, packages, and system dependencies for various Linux distributions.   

## Usage

Run the script with appropriate privileges (typically `root` or via `sudo`):

```bash
sudo ./<script-name>.sh
```
